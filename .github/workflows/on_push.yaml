name: Github Actions On Push
on: push

jobs:
  on-push-job:
    runs-on: ubuntu-latest

    steps: 
      - name: First Step
      run: |
        echo "this is a step within a job within a workflow"
        ls -lart
