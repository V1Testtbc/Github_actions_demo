name: activity filters
on: 
  workflow_dispatch:
  pull_request:
    types: [synchronize]

jobs:
  on-push-job:
    runs-on: ubuntu-latest

    steps: 
      - name: any old step
        run: echo "this is a thing"